# Copy these lines into the Laravel .env on the VPS
# Then run: php artisan optimize:clear && php artisan config:cache && php artisan route:cache

GOOGLE_MAPS_API_KEY=
GOOGLE_MAPS_SERVER_KEY=

FIREBASE_PROJECT_ID=
FIREBASE_WEB_API_KEY=
FCM_SERVER_KEY=
